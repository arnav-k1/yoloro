# Get a key from https://console.cloud.google.com/apis/credentials after enabling the
# "YouTube Data API v3" on a Google Cloud project. Without this set, yoloro runs in demo
# mode using a small curated set of real videos so the UI is still fully testable.
YOUTUBE_API_KEY=
